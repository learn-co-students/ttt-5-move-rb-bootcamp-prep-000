#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = Array.new(9, " ")
move = "X"
puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"
display_board(board)
index = gets.strip
move(board,input_to_index(index),move)
