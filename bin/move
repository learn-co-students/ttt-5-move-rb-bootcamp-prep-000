#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

puts "Welcome to Tic Tac Toe!"
board = [" "] * 9
puts "Where would you like to go?"

id = gets.strip
index = input_to_index(id)

 move(board,index)
display_board(board)
