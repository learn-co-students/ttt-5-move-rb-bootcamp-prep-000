#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts 'Welcome to Tic Tac Toe!'
move_input = gets('What is your move?').strip

index = input_to_index(move_input)

move(board, index)

display_board(board)